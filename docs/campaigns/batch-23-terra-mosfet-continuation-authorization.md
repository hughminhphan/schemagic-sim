# Batch 23 Terra MOSFET continuation authorization

Date: 2026-08-23.

## User direction

Hugh directed: “so keep going - why is it failing all the time - maybe we can step up to terra instead of luna? maybe there is some issue with he plan - review the plan and the k epepgoing”.

This direct instruction authorizes continued execution toward 1,000 reviewed packages after an honest no-fit Batch 22 result. It supersedes only the sequencing clause in `docs/campaigns/scale-2k-terra-recovery-authorization.md` that conditioned the next batch on the previous batch passing. It does not relax any evidence, source-mode, threshold relationship, provenance, fit, hard-bound, collision, package, independent-review, promotion, release, deployment, or publishing gate.

## Batch 22 terminal disposition

Batch 22 fixed denominator 40 cannot reach its prefit admission gate and must receive no fit:

- nine candidates are topology parks;
- ten earlier candidates are terminal evidence or execution failures;
- order 1019 is accepted and strong through the reviewed evidence-envelope path;
- order 1028 is terminal because its source threshold uses fixed VDS = -10 V rather than the required VDS = VGS relationship; and
- order 1007 is terminal because its cited RDS(on) table does not state a test mode or pulse timing.

The remaining 18 uncalled candidates could raise the maximum accepted count only to 19, below the unchanged 20-of-40 prefit gate. No further Batch 22 extraction call and no fit are authorized. Its immutable ledgers, responses, envelopes, translated extractions, PDFs, jobs, and terminal dispositions must remain preserved.

## Batch 23 fixed tranche

Batch 23 is the exact contiguous frozen MOSFET range 1040 through 1079, fixed denominator 40, zero substitutions. Its identity vector is derived directly from frozen `tools/part-feeder/data/manifests/scale-2k.json`, SHA-256 `c003a37a26c5ce343ed884a38801d9a5acdbac17412d409dc2fd9c1b5ddf790e`; the canonical `{lcsc_id, mpn, conveyor_family, family_rank, frozen_campaign_order}` vector SHA-256 is `b8194ab761763aa012aab8173b33998898084faae9574844a9ec5d4b640d3638`.

Use fresh ignored root `tools/conveyor/data/batch-23-terra-mosfet-scale/`. Slice the freeze directly; do not query, rebalance, substitute, or reuse an earlier response. Complete canonical PDF acquisition and topology preflight before any model call. Unsupported topology remains in the denominator and receives zero calls.

Every eligible target uses the reviewed flat evidence-envelope producer and deterministic strict-schema translator at exact tracked hashes. Terra inspects exactly one PDF per turn. Each target may receive one initial extraction and, only if warranted, one focused repair; no completed call may be repeated. Exact source facts remain mandatory: two-sided threshold evidence with VDS = VGS or an admissible cited typical, and RDS(on) typical or inclusive maximum with stated temperature, VGS, ID, source role, locator, magnitude convention, and source test mode. Missing facts fail closed.

Run no fit unless at least 20 of 40 candidates pass pure factory evidence preflight and at least 10 are strong routes (`curve-fitted`, `typ-point`, or genuine `interval-constrained`). If either gate becomes mathematically unreachable, seal the batch without further model calls or fit and continue to the next untouched contiguous tranche under a fresh tracked continuation record.

If both prefit gates pass, run exactly one isolated fit pass with no retry or family parking. Final PASS remains at least 14 staged packages, including at least 10 strong, followed by unchanged independent package review, collision audit, native/WASM gates, deterministic promotion, and release audit. Only approved packages enter the reviewed library.
