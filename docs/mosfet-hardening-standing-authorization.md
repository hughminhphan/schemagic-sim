# MOSFET hardening standing authorization

Date: 2026-08-13.

Hugh explicitly directed: “keep going mate you do not need my authorization”. This replaces the per-cycle authorization requirement for the active MOSFET evidence-admission work and authorizes autonomous bounded corrections until one of these terminal outcomes:

1. the integrated evidence-admission implementation receives independent approval and Batch 15 proving can run, or
2. a material architectural or electrical-model limitation remains that cannot be corrected without relaxing existing truth gates.

## Operating rules

- Continue through small review-found consistency defects without asking Hugh after each cycle.
- Keep each correction narrow and consequence-driven.
- Luna owns semantic datasheet interpretation, row and curve grouping, footnote resolution, and ambiguity reporting.
- Deterministic code owns unit normalization, content-addressed identity generation and verification, inclusive published bounds, native simulation, collision checks, and compact reproducible provenance.
- Do not add representational ceremony without a concrete wrong electrical or provenance outcome.
- Do not relax fit residual gates, published hard bounds, evidence completeness, citation truth, provenance, collision rules, independent review, or package validation to improve yield.
- Unsupported pulse evidence remains excluded from static DC claims unless an equivalent pulse fitting and bench path is deliberately implemented and reviewed.
- The reviewed library remains unchanged until candidate packages pass independent package review.

## Review loop

After each narrow correction:

1. Run focused mutation or electrical regressions.
2. Run full model-factory, component-schema, reviewed-library, workspace, and native/WASM gates.
3. Run an independent read-only adversarial review.
4. If review finds a small concrete consistency defect, correct it and re-review without asking Hugh.
5. Stop only for a material architectural blocker or after approval.

This is not permission for unbounded speculative redesign. Corrections remain directly tied to reproduced failures.

## Batch barrier

Hardening writers and reviewers remain blind to the ignored Batch 15 evidence root. After integrated code approval, the coordinator may open Batch 15 only to perform the already-authorized hash and ledger reverification and exactly one fit pass. Batch 16 remains blocked until Batch 15 proving passes.

The scheduler upgrade remains closed. Extraction concurrency remains capped at four unless separately corrected and independently approved under this standing direction.

No repository push, deployment, promotion, launch post, or community publication is implied by this authorization. Existing release and explicit publishing gates remain in force.
