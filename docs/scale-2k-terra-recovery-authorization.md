# scale-2k Terra recovery authorization

Date: 2026-08-23.

## User direction

After Batch 16 failed its unchanged proving gate, Hugh directed: “so keep going - why is it failing all the time - maybe we can step up to terra instead of luna? maybe there is some issue with he plan - review the plan and the k epepgoing”.

This is the direct post-failure redirect required by `docs/batch-16-proving-execution.json`. It authorizes a plan correction and continued execution toward 1,000 reviewed packages. It does not relax any electrical, provenance, citation, hard-bound, collision, package, independent-review, promotion, deployment, or publishing gate.

## Why the previous plan could not reach the goal reliably

The reviewed library remains 710, so 290 additional promotions are required. After retired order 999, the old authorization left only 306 untouched MOSFET rows, orders 1000 through 1305. Reaching 1,000 from that pool alone required promoting 94.77% of every remaining row. That is incompatible with unavoidable topology parks, identity collisions, incomplete primary evidence, fit failures, and normal independent-review attrition.

Batch 16 also demonstrated a deterministic contract defect rather than a simple Luna-quality failure:

- all 10 Luna responses passed conveyor ingest;
- six were classified as curve-bearing F2 candidates;
- model-factory staged zero because it reparsed legacy free-text conditions before honoring the newly required structured curve semantics;
- structured condition fields covered curves but not threshold and RDS(on) table evidence; and
- production-shaped tests duplicated the exact prose expected by the parser, masking the real producer/consumer seam.

Terra replaces Luna for new semantic extraction, but a model swap alone is insufficient and is blocked until the typed contract correction below is independently approved.

## Candidate pool correction

The sole source remains frozen `tools/part-feeder/data/manifests/scale-2k.json`, SHA-256 `c003a37a26c5ce343ed884a38801d9a5acdbac17412d409dc2fd9c1b5ddf790e`.

This authorization reopens the 190 untouched deferred diode orders 690 through 879. They were scheduling deferrals, not permanent technical condemnations: no PDF was downloaded, no extraction call ran, and no fit was attempted. Their blocker was the diode F1 fallback around published maxima. A further code audit found that the current package path preserves a published maximum as a bound, but its F1 parameter derivation still uses an undocumented 95% current / 97% voltage interior surrogate and records no honest bound-only calibration mode. Candidate work is therefore also blocked until the narrow provenance correction below distinguishes direct evidence from a maximum-only constraint and rejects seed-only preflight admission.

The fresh candidate pool is therefore:

- deferred untouched diodes 690..879: 190 rows;
- untouched MOSFETs 1000..1305: 306 rows;
- total: 496 rows.

The campaign needs 290 promotions from 496 candidates, a 58.47% aggregate yield. Orders 880..999 remain retired and no old candidate response, supplement, fit, or package may be reused.

## Narrow evidence correction

Before any new PDF download or semantic extraction, one reviewed correction may change only the MOSFET producer/consumer evidence boundary, the family-wide pure preflight validator, the diode F1 calibration provenance record, and directly related tests and fixtures:

- structured condition semantics become canonical for curves while raw `test_conditions` remains preserved as audited source text;
- exact parseable contradictions between source text and structured facts still fail, but unknown descriptive prose cannot veto otherwise complete typed evidence;
- structured `magnitude_convention` carries P-channel signed/absolute meaning; axes remain exact `VGS`, `VDS`, and `ID` aliases;
- threshold and RDS(on) scalar evidence receive typed temperature, electrical relationship/value, source test mode, magnitude convention, and existing structured locator fields;
- source test mode `not_stated` is admissible only for the existing fixed gate-threshold policy and may never be rewritten as a claim that the PDF stated DC;
- missing temperature, missing threshold `VDS = VGS`, missing two-sided interval evidence, missing RDS(on) bias/current, malformed locators, or typed/source contradictions remain fail-closed;
- pulsed and single-pulse curves remain excluded from static F2 fitting; and
- a pure no-fit/no-stage candidate-evidence validator must run the same normalizer and admission checks used by the fitter.

For diodes, the same correction must:

- reject seed-only preflight admission and require at least one positive SI forward-voltage/current pair from the extraction;
- classify direct typical or digitized-curve evidence separately from maximum-bound-only evidence;
- keep a published maximum as an inequality constraint with zero residual targets, never relabel it as a typical observation; and
- record the conservative bound-only parameter policy and its source constraint explicitly instead of reporting a catalog-parametric fallback.

Python fit physics, F2 error gates, hard bounds, model forms, collision logic, package admission, native ngspice, and native/WASM parity are unchanged.

Required regressions include opaque but preserved source prose with complete typed facts, parseable contradictions, P-channel magnitude handling, pulsed-to-static rejection, threshold `not_stated` policy, missing temperature, missing interval endpoint, missing `VDS = VGS`, malformed locators, one production fixture crossing conveyor through the pure factory evidence validator, and diode preflight cases for direct, maximum-bound-only, and seed-only evidence.

The correction requires focused suites, full conveyor and model-factory suites, all 710 reviewed packages, workspace tests/typechecks/build, and independent read-only adversarial review before candidate work.

## Terra recovery tranche

The first recovery tranche is exact contiguous frozen diode orders 690 through 729, fixed denominator 40, zero substitutions, fresh ignored root `tools/conveyor/data/batch-17-terra-diode-proving/`.

1. Slice directly from the frozen manifest; do not query or rebalance the catalog.
2. Download fresh canonical PDFs and complete topology preflight before extraction. Unsupported topology parks remain in the denominator and receive no model call.
3. Create immutable per-order jobs and an append-only unique completion ledger.
4. Run at most four concurrent Terra extraction lanes. Every prompt begins `Do not invoke any Skill at any point in this task.`
5. Permit one focused discrepancy repair per target and one missing-only infrastructure replacement; never repeat a completed call.
6. Require every row to receive exactly one terminal prefit disposition: topology parked, extraction failed, conveyor accepted then evidence-validator rejected, or evidence-validator accepted.
7. Run no fit unless at least 20 of 40 rows pass the pure factory evidence validator. A lower yield stops before fitting and records evidence-source viability rather than consuming a fit pass.
8. If the prefit gate passes, run exactly one fit pass with no retry or family parking.
9. Final PASS requires at least 14 provenance-clean staged packages of 40, including at least 10 whose admitted calibration evidence is direct typical/curve evidence or a genuine typed interval—not an inequality-only maximum converted to equality.
10. PASS receives independent package review, collision audit, native/WASM validation, deterministic promotion, and final release audit. Only approved packages enter the reviewed library.

After PASS, continue contiguous 40-row Terra diode batches through order 879, then contiguous 40-row Terra MOSFET batches from order 1000. Every batch uses the same prefit evidence gate and final 14/10 scale gate. Stop at 1,000 reviewed packages or frozen-pool exhaustion.

## Isolation and release control

Content inside `.claude/`, `tools/conveyor/data.pre-hardening/`, `tools/conveyor/data/batch-16-prefit/`, and ignored Batch 10 through 16 candidate roots remains prohibited for reuse. Tracked terminal summaries may be read for diagnosis.

Unrelated Designer/schema work currently present in the primary worktree belongs to the user and must remain untouched. Correction review and candidate execution use an exact isolated tracked worktree whenever those edits would otherwise contaminate the approved tree.

Repository push, library promotion, and deployment remain blocked until the exact package-review and release-audit chain approves them. Public launch and community posts remain separately approval-gated.
