{
  "name": "@opencircuit/conveyor",
  "private": true,
  "version": "0.1.0",
  "description": "Resumable bulk fitting harness for SQL-selected discrete model tranches",
  "scripts": {
    "test": "python3 -m unittest discover -s test -p 'test_*.py' -v",
    "typecheck": "python3 -m compileall -q conveyor conveyorlib.py terra_envelope.py test"
  }
}
