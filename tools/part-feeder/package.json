{
  "name": "@opencircuit/part-feeder",
  "private": true,
  "version": "0.1.0",
  "description": "Local-only bulk catalog and datasheet feeder for original model fitting",
  "scripts": {
    "test": "python3 -m unittest discover -s test -p 'test_*.py' -v",
    "typecheck": "python3 -m compileall -q feeder feederlib.py test"
  }
}
