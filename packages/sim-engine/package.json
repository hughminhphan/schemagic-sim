{
  "name": "@opencircuit/sim-engine",
  "version": "0.0.1",
  "private": true,
  "description": "Worker-isolated ngspice WebAssembly runtime for scheMAGIC Simulator",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./provenance-testing": "./src/provenance.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencircuit/circuit-schema": "*"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "vitest": "3.2.7"
  }
}
