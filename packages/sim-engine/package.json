{
  "name": "@opencircuit/sim-engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/worker.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencircuit/circuit-schema": "*",
    "@opencircuit/signal-workbench": "*"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "vitest": "3.2.4"
  }
}
