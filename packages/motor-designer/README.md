# scheMAGIC Motor Designer

Deterministic scheMAGIC Motor Designer recipes for one brushed-DC motor and one H-bridge. The public entrypoint is `generateMotorDesign`; the generic pipeline remains owned by the internal `@opencircuit/design-engine` compatibility module.

The installed V2/V3 production generator uses bundled reviewed release `2026-08-27.2`: 24 exact profiles, including two reviewed facts-V3 TVSes and three reviewed facts-V3.2 integrated bridges. Nineteen profiles are Motor-relevant and facts-schema-compatible with at least one installed or frozen Motor recipe contract; this readiness count is not candidate materialization, and DRV8262 is excluded by the installed recipe's capacitor-network boundary. The added shared 22 uF MLCC is consequently visible to the generic integrated Cartesian enumeration, increasing the DRV8876 subset from eight to ten options, but exact recommendation gates preserve the same selected-part outcome, DRV8262 contributes no generated options, and the external role-qualified recipe remains at 54 options. The legacy `generateMotorDesign` compatibility path remains shaped by its synthetic M1/M2 fixture catalog; synthetic identities and evidence are never presented as reviewed or orderable manufacturer parts.

Separately, `@opencircuit/motor-designer/reviewed-real` exports seven staged primary-source transcriptions. Those staged records remain excluded from generation and do not claim technical completeness. The exact DRV8262DDVR, DRV8876PWPR, and STSPIN840 identities have separately completed facts-V3.2 normalization, independent review, and reviewed-library admission. DRV8262DDVR is admitted at `sha256:a6239ab49665a69a9e54c0f4ecd103f7fdcfdf5f6cf29685baf03a1dc4c41a4a`; its exact-part TI WEBENCH BXL supplies a calculated Most/Density-A TOP-copper footprint bound. That catalog state does not establish generation feasibility: the current one-local-capacitor role cannot represent its two VM bypass positions plus separate charge-pump/regulator network, so DRV8262 remains excluded from recipe candidate materialization, customization targets, and browser generation.

That subpath also exports deterministic facts-V2 candidate-profile plans for the three exact full-bridge gate-driver MPNs used by the external-NMOS topology lane. Their pinned datasheet bytes bind authored package-name candidates and the manufacturer maximum package heights (DRV8701ERGER 1 mm, A3941KLPTR-T 1.20 mm, and HIP4081AIBZ 2.65 mm). Mounted board area remains blocked: the DRV8701 source has no dimensioned recommended land pattern, while the A3941 and HIP4081A reference drawings have not been converted into independently reviewed maximum bounding-box projections.

The complete facts-V2 draft-authoring assessment classifies every required target path for all seven profiles as missing-source, semantic-mismatch, mounted-geometry, or independent-review work. It ranks DRV8701ERGER as the strongest current evidence position with six draft blockers: one missing source fact, four semantic mismatches, and one mounted-geometry gap. Twenty additional source-bound paths remain pending independent review. DRV8262DDVR closes the staged primary-source 4.5 V to 60 V and continuous-current coverage gaps with a 20 A RMS DDV single-H-bridge recommended operating limit. Its 32 A value remains a protection threshold, its 200 kHz statement remains non-guaranteed application guidance, and its two VM bypass capacitors plus separate charge-pump/regulator network cannot be represented by the current one-local-capacitor recipe. The report separately records the integrated tranche's authored normal peak-current ceiling as 3.5 A. The module-wide stall/peak envelope remains blocked through 30 A: the external-FET H-bridge topology is the intended high-current path, but stall duration, pulse duty, MOSFET safe-operating-area, protection response, and transient-thermal evidence are not jointly bound. Across application-envelope and integrated-lane coverage, the report therefore retains exactly two detailed blockers: the 30 A stall/peak evidence gap and hard PWM evidence. Selection is not admission: the assessment retains `decision: no_honest_draft`, `draft: null`, pending independent review, and isolated admission for every profile. It does not alter generator behavior or claim simulation fidelity.

STSPIN840 evidence can be independently recaptured without placing vendor bytes in the repository. Run `npm run evidence:capture -- capture /absolute/path/to/new-directory` from this package on a network that can retrieve both official ST URLs, then replay it with `npm run evidence:capture -- verify /absolute/path/to/new-directory/stspin840-evidence-capture.json`. The capture refuses repository-local output, official-host/path redirects are checked exactly, HTML/PDF media identity is checked, and verification recomputes both SHA-256 values from the saved bytes. It does not edit the staged catalog, admission ledger, or production release.

The V1/V2 compatibility recipes and former mixed facts-V2/V3/V3.1 external-NMOS recipes remain frozen and directly testable. The installed production set exposes `motor.native.integrated-h-bridge.facts-v3-2@3.2.6` (`sha256:1ffaf03fc1778cb1b287e3f48c6d0fc82eb91b2d6f28b76f2fc500941acb2d07`) plus `motor.native.external-nmos-h-bridge.facts-v3-1-role-qualified@3.1.7` (`sha256:e526bba9ce25114b505264e7d281607ee223c10de19e795780a64f04617c0947`) as engine-owned code; callers receive only immutable refs, never recipe callbacks. External predecessor `3.1.6` remains frozen at `sha256:93e6306249d0b8376a214c8b8a2dd6c7058e17cf9fb907e91ac8082552a05320`, with `3.1.5` frozen at `sha256:ef1b07d8b547bf4d46ce2bc76943059e8fa597d52d63e4b62d9d5c4de0bc2187`; integrated predecessors `3.2.5`, `3.2.4`, and `3.2.3` remain frozen at `sha256:75e1ea8fa6c3c4fadd44187b9134a2e61840d2ad5b0123d0bbaff17a910dce1a`, `sha256:b33804be0fd68ac15bde76ce46db501325dac5030c5b13f7916cd8362c853d84`, and `sha256:86d3e6fed563d7e663d74f692286a2287b2932afea198fe76dc86eab07c50ece`, respectively. The external successor preserves the exact reviewed `MIC4606-2YML-T5` direct xHO/xLO structure, no populated series-gate-resistor role, and the separate source-bound bootstrap and VDD-local capacitor roles. It exact-binds the reviewed Diodes Incorporated `3.0SMCJ33CAQ` profile (`sha256:f67d5716b2900039b09040038e3e5c8c059bf19edd12cf3776145c9f46097474`): the 33 V stand-off clears the 30 V bus only when the declared ambient is covered by its exact 25 °C source condition, while the 53.3 V maximum published clamp is below the 60 V MOSFET and 90 V xHS absolute limits only at the exact 25 °C, 56.3 A, non-repetitive 10×1000 µs source condition. At the M2 ambient, `motor.external.tvs-stand-off` therefore fails closed as unknown. The application transient current, waveform, energy, overshoot, parasitics, avalanche/SOA, thermal behavior, and full TVS coordination remain unproved. The candidate still has no implemented VDD driver-bias rail, so `motor.external.driver-bias-source` remains a required unknown alongside the existing capacitor, gate-network, switching, thermal, transient, and simulation gaps.

The `@opencircuit/motor-designer/v3` subpath exposes a named production constraint observation under Motor policy `sha256:6a1ca0c0b1476163daff6e52724605461b5185a10ffe36dd06642caf59ac45f0` and Motor context `sha256:06a4ef8b8141852bf9506c6f4f632a7b349b0947c449f85172313380dc195d38`. The integrated observation is pinned to result `sha256:5d3073a4e68e71f60f2d9eeaabb2ca90da213a3794c6a6779ad83eeefd703044`, execution `sha256:34a59924931a3d6200594670374c5e6d57f07e4722b9d7a92736a0001adc79e4`, candidate `candidate:v2:sha256:3f9953a5582e56cd999070367f1b3c4830bfad4d4e9df55e2ce91891fb5cb16e`, and ineligible decision `sha256:27aabbc0fc3d812752e803d3ce15d40457572b2faa1f81def3a8f52ff6d05276`. Every exact DRV8262 combination is separately rejected in match before materialization; none becomes a candidate or customization target. For M2, strict generation is pinned to result `sha256:b0bf69fc7bac1accbaf0232204f14ae243bb59d6401b979b370e2b40b1e65a77` and execution `sha256:a776a7eea754a7a7724d4df2663693f11eaeb3485784b7d2e7e9e0b7107590da`: it checks and rejects all 54 options. Permissive structural inspection is pinned to result `sha256:01b56be6e6dfc3ca46bb36550f6999571d19bd109e73e99d29d308a69a7733b3`, execution `sha256:5b45a733cea233ab9c9c36603747e623e2cb6031dfbb4b22c1020cff86af1fce`, retained candidates `candidate:v2:sha256:a118ec185d3bbdd54360c94dc6a45476dfdae4f1d6ffb2ac0f6695e485a30152` and `candidate:v2:sha256:fce7b8a1f83bd1e305e12392a16d8f337e06106c66482640338cf03acdc12382`, and ineligible decision `sha256:f7dafa7fd6397b7a3fcfe43f12a93e0b05017faa0f91d25ae846584c5afe0604`; each retained candidate has 9 satisfied and 21 blocked required rules, and 52 materialized options are Pareto-rejected. None of these deterministic identities is a verification, simulation, switching, or release-eligibility claim.

The browser authorizes only the exact in-process generation object issued by its installed Motor adapter and fingerprints the complete V2 result, execution report, context hash, and V3 decision before rendering production policy state. Electrical JSON and share payloads remain canonical V2 result bytes only; a share gains V3 policy UI only after explicit exact installed regeneration, while file imports and demonstrations remain structural-only. The external lane now has candidate identities for structural inspection, but those identities remain policy-ineligible and transfer no sourcing, selected-semiconductor, physical-feasibility, or simulation authority. For an authorized integrated source/target pair, the portable inspection receipt contains the exact customized-result sidecar and fixed BOM/SVG descriptors by kind, filename, MIME type, UTF-8 byte length, and SHA-256; it does not contain either artifact payload, requires exact replay, and transfers no installed-context or production authority.

This readiness result is deliberately narrower than overall Designer release readiness, which remains blocked. The `3.2.6` successor inherits the exact reviewed DRV8876 input truth table—coast, reverse, forward, and low-side slow-decay brake—in `motor.integrated.operating-modes` only when PMODE is sampled high at device power-up. It also inherits the `motor.integrated.local-capacitance-nominal` pass for the exact hash-bound DRV8876PWPR/C1608X7R1H104K080AA physical BOM pairing: the selected capacitor's reviewed 100 nF nameplate value exactly matches TI's 0.1 µF recommendation. Its only new behavior is the exact evidence-bound DRV8262 companion-network rejection before component materialization. None of these facts proves a required-minimum, tolerance floor, effective capacitance, derating, voltage suitability, placement, ripple, transient, or switching-safety pass. The retained integrated observation remains STSPIN840, for which the mode and nominal-local-capacitance rules stay unknown. DRV8876PWPR continuous-current capability, a guaranteed minimum input pulse, configured current-limit behavior, physical pin/wiring semantics, braking energy, loss/thermal/SOA/transient analysis, and selected-part simulation fidelity remain unknown. Strict requests may therefore produce no candidates; callers must explicitly allow unknown hard constraints to inspect deterministic observations. The external observation likewise retains `motor.external.gate-network`, `motor.external.bootstrap-capacitance`, `motor.external.local-capacitance-effective`, `motor.external.local-voltage-rating`, `motor.external.capacitor-placement`, `motor.external.bulk-capacitance`, `motor.external.passive-derating`, and `motor.external.switching-and-loss` as blocking unknowns. The exact source supports the direct structure and nominal capacitor floors, but does not prove switching behavior, effective capacitance, QGATE or IHBS*tON closure, refresh or leakage, physical placement, bulk energy, thermal behavior, or selected-part simulation fidelity. Regenerating the data-only status snapshot cannot admit profiles, enable providers, open the browser production workflow, or establish simulation fidelity.

The package produces analytical candidates and inspectable rejection traces. The installed facts-V3.2.6 integrated recipe and facts-V3.1.7 TVS-voltage-qualified external recipe define a connected exact-BOM `assembly` as the default structural graph, then a separate request-derived **averaged operating-point** graph: a nominal-supply-times-duty DC source, the request-declared winding resistance, and an algebraically closed operating-point back-EMF source. The external selected BOM contains the exact driver, four MOSFETs, four 100 kΩ pull-downs, two bootstrap MLCCs, one independently bound VDD-local MLCC, one bulk capacitor, current shunt, and the exact `3.0SMCJ33CAQ` TVS, but no series-gate resistor; its structural graph uses an explicit `gate-drive-direct-to-bridge` connection. The companion behavioral graph contains no MPNs or selected-part design blocks; every selected BOM line is explicitly recorded as not represented by that graph.

This behavioral circuit is intentionally not a switching or selected-silicon model. It does not express PWM edges, gate drive, dead time, speed/torque dynamics, protection, thermal behavior, or PCB parasitics. Only `pwm_loaded_steady_state` carries behavioral coverage. Startup, current-limit/stall, and braking remain unavailable, and this package makes no native-ngspice/WASM numerical-validation claim. In an exact installed browser context, that zero-omission scenario can be downloaded as Scenario SPICE; the deck cannot change the structural BOM, constraints, ranking, persisted evidence, receipts, or V3 eligibility. Simulation CSV and Simulator handoff remain disabled without actual pinned-engine samples and a matching receipt.
